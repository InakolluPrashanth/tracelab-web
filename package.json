{
  "name": "tracelab-free-launch",
  "version": "2.0.0",
  "private": true,
  "description": "TraceLab free launch build with Render + OneCompiler backend",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0"
  }
}
